# Copyright (c) 2025-2026 Guy Erreich
#
# SPDX-License-Identifier: MIT
name: 'GH Release'
description: 'Creates a GitHub Release based on the current tag'
inputs:
  github_token:
    description: 'GitHub Token for creating releases'
    required: false
runs:
  using: "composite"
  steps:
    - name: Determine Release Details
      id: details
      shell: bash
      run: |
        # Get the exact tag for the current commit. Fails if no tag.
        TAG=$(git describe --tags --exact-match)
        echo "Found tag: $TAG"
        echo "tag=$TAG" >> $GITHUB_OUTPUT
        
        if [[ "$TAG" == *"-rc"* ]] || [[ "$TAG" == *"-dev"* ]]; then
          echo "is_prerelease=true" >> $GITHUB_OUTPUT
          echo "make_latest=false" >> $GITHUB_OUTPUT
        else
          echo "is_prerelease=false" >> $GITHUB_OUTPUT
          echo "make_latest=true" >> $GITHUB_OUTPUT
        fi

    - name: Prepare Release Notes
      shell: bash
      run: |
        if [ -f "CHANGELOG.md" ]; then
          # Extract content starting from the first version header
          # and stop before the License section if it exists
          # Use sed to print from the first version header line until the License section or end of file
          sed -n '/^## \[/,$p' CHANGELOG.md | sed '/^## License/Q' > RELEASE_NOTES.md
          # Prevent GitHub from autolinking bare @v1 / @v2… to squatted usernames
          # in release notes (e.g. github.com/v1). Keep the floating git tag unchanged.
          sed -i -E 's/@v([0-9]+)/`v\1` tag/g' RELEASE_NOTES.md
          echo "notes_file=RELEASE_NOTES.md" >> $GITHUB_OUTPUT
        else
          echo "CHANGELOG.md not found, falling back to auto-generated notes"
          echo "notes_file=" >> $GITHUB_OUTPUT
        fi
      id: notes

    - name: Create GitHub Release
      shell: bash
      env:
        GH_TOKEN: ${{ inputs.github_token || github.token }}
        TAG: ${{ steps.details.outputs.tag }}
        PRERELEASE: ${{ steps.details.outputs.is_prerelease }}
        LATEST: ${{ steps.details.outputs.make_latest }}
        NOTES_FILE: ${{ steps.notes.outputs.notes_file }}
      run: |
        NOTES_ARG=""
        if [ -n "$NOTES_FILE" ] && [ -f "$NOTES_FILE" ]; then
          echo "Using release notes from $NOTES_FILE"
          NOTES_ARG="--notes-file $NOTES_FILE"
        else
          echo "Using auto-generated release notes"
          NOTES_ARG="--generate-notes"
        fi
        
        gh release create "$TAG" \
          $NOTES_ARG \
          --prerelease="$PRERELEASE" \
          --latest="$LATEST" \
          --title "$TAG"
